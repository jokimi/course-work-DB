<UserControl x:Class="PodSproutFM.UserControls.PodcastUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PodSproutFM.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Margin="10" CornerRadius="10" Padding="30 15" Background="{StaticResource OffWhiteBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0 15">
                    <Image x:Name="topicCover" MaxHeight="150" MaxWidth="150" Stretch="UniformToFill">
                        <Image.Clip>
                            <RectangleGeometry RadiusX="20" RadiusY="20" Rect="0,0,150,150"/>
                        </Image.Clip>
                    </Image>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="30 0 20 0">
                    <TextBlock FontSize="35" x:Name="blockPodcastName" Text="PODCAST'S NAME" FontFamily="Architun" Margin="0 0 0 10"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Диктор:" Margin="0 0 5 0" FontFamily="Architun" Foreground="#6d8960"/>
                        <TextBlock x:Name="blockNarratorName" Text="NARRATOR'S NAME" FontFamily="Architun"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Тема:" Margin="0 0 5 0" FontFamily="Architun" Foreground="#6d8960"/>
                        <TextBlock x:Name="blockTopicName" Text="TOPIC'S NAME" FontFamily="Architun"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Год:" Margin="0 0 5 0" FontFamily="Architun" Foreground="#6d8960"/>
                        <TextBlock x:Name="blockYear" Text="YEAR" FontFamily="Architun"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <Button Name="playPodcast" Content="Включить" IsEnabled="True" Visibility="Visible" Margin="0 0 0 5" VerticalAlignment="Center" Click="PlayPodcast_Click"/>
                    <Button Name="savePodcast" Content="В избранное" IsEnabled="True" Visibility="Visible" Margin="0" VerticalAlignment="Center" Click="SavePodcast_Click" />
                </StackPanel>
                <StackPanel x:Name="adminButtons" Grid.Column="3" Margin="20 0 0 0" Visibility="Collapsed" VerticalAlignment="Center">
                    <Button Name="editPodcast" Content="Изменить" IsEnabled="True" Visibility="Visible" Margin="0 0 0 5" Click="EditPodcast_Click"/>
                    <Button Name="deletePodcast" Content="Удалить" IsEnabled="True" Visibility="Visible" Margin="0" Click="DeletePodcast_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>